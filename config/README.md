# Local Config Templates

This folder stores local runtime config templates only.

Rules:
- Keep only `*.example.env` in git.
- Do not commit real `*.env` files or secrets.
- Copy an example file to `*.env` for local deployment.
