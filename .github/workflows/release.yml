name: release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Build linux/amd64
        env:
          CGO_ENABLED: "0"
        run: |
          mkdir -p dist
          GOOS=linux GOARCH=amd64 go build -trimpath -ldflags "-s -w -X main.version=${{ github.ref_name }}" -o dist/https-proxy_gateway_${{ github.ref_name }}_linux_amd64 ./cmd/gateway
          GOOS=linux GOARCH=amd64 go build -trimpath -ldflags "-s -w -X main.version=${{ github.ref_name }}" -o dist/https-proxy_agent_${{ github.ref_name }}_linux_amd64 ./cmd/agent
          cp README.md dist/https-proxy_README_${{ github.ref_name }}.md
          sha256sum dist/https-proxy_gateway_${{ github.ref_name }}_linux_amd64 dist/https-proxy_agent_${{ github.ref_name }}_linux_amd64 dist/https-proxy_README_${{ github.ref_name }}.md > dist/https-proxy_${{ github.ref_name }}_SHA256SUMS
      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/https-proxy_gateway_${{ github.ref_name }}_linux_amd64
            dist/https-proxy_agent_${{ github.ref_name }}_linux_amd64
            dist/https-proxy_README_${{ github.ref_name }}.md
            dist/https-proxy_${{ github.ref_name }}_SHA256SUMS
